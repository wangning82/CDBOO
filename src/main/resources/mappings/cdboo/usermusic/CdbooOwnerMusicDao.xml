<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cdboo.usermusic.dao.CdbooOwnerMusicDao">
    
	<sql id="cdbooOwnerMusicColumns">
		a.id AS "id",
		a.music_id AS "musicId.id",
		a.user_id AS "user.id",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag",
		a.status AS "status"
	</sql>
	
	<sql id="cdbooOwnerMusicJoins">
	</sql>
    
	<select id="get" resultType="CdbooOwnerMusic">
		SELECT 
			<include refid="cdbooOwnerMusicColumns"/>
		FROM cdboo_owner_music a
		<include refid="cdbooOwnerMusicJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="CdbooOwnerMusic">
		SELECT 
			<include refid="cdbooOwnerMusicColumns"/>
		FROM cdboo_owner_music a
		<include refid="cdbooOwnerMusicJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="musicId != null and musicId.id != null and musicId.id != ''">
				AND a.music_id = #{musicId.id}
			</if>
			<if test="user != null and user.id != null and user.id != ''">
				AND a.user_id = #{user.id}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.create_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="CdbooOwnerMusic">
		SELECT 
			<include refid="cdbooOwnerMusicColumns"/>
		FROM cdboo_owner_music a
		<include refid="cdbooOwnerMusicJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.create_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO cdboo_owner_music(
			id,
			music_id,
			user_id,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag,
			status
		) VALUES (
			#{id},
			#{musicId.id},
			#{user.id},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag},
			#{status}
		)
	</insert>
	
	<update id="update">
		UPDATE cdboo_owner_music SET 	
			music_id = #{musicId.id},
			user_id = #{user.id},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks},
			status = #{status}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE cdboo_owner_music SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
	<update id="remove">
		DELETE FROM cdboo_owner_music WHERE id = #{id}
	</update>
	
</mapper>