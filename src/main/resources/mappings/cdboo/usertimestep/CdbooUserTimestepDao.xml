<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cdboo.usertimestep.dao.CdbooUserTimestepDao">
    
	<sql id="cdbooUserTimestepColumns">
		a.id AS "id",
		a.user_id AS "user.id",
		a.name AS "name",
		a.start_time AS "startTime",
		a.end_time AS "endTime",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag",
		u2.name AS "user.name"
	</sql>
	
	<sql id="cdbooUserTimestepJoins">
		LEFT JOIN sys_user u2 ON u2.id = a.user_id
	</sql>
    
	<select id="get" resultType="CdbooUserTimestep">
		SELECT 
			<include refid="cdbooUserTimestepColumns"/>
		FROM cdboo_user_timestep a
		<include refid="cdbooUserTimestepJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="CdbooUserTimestep">
		SELECT 
			<include refid="cdbooUserTimestepColumns"/>
		FROM cdboo_user_timestep a
		<include refid="cdbooUserTimestepJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="user != null and user.id != null and user.id != ''">
				AND a.user_id = #{user.id}
			</if>
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
			<if test="notIncludeIds != null">
				AND a.id not in
				<foreach item="item" index="index" collection="notIncludeIds" open="(" separator="," close=")">  
  					${item}  
 				</foreach>  
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.create_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="CdbooUserTimestep">
		SELECT 
			<include refid="cdbooUserTimestepColumns"/>
		FROM cdboo_user_timestep a
		<include refid="cdbooUserTimestepJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.create_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO cdboo_user_timestep(
			id,
			user_id,
			name,
			start_time,
			end_time,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{user.id},
			#{name},
			#{startTime},
			#{endTime},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE cdboo_user_timestep SET 	
			user_id = #{user.id},
			name = #{name},
			start_time = #{startTime},
			end_time = #{endTime},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE cdboo_user_timestep SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>